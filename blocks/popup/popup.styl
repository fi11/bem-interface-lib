.popup
    background: #fff
    box-shadow: 0 0 0 1px rgba(0,0,0,.1), 0 10px 20px -5px rgba(0,0,0,.4)
    position: absolute
    box-sizing: border-box
    z-index: 99

    &.popup_hide
        visibility: hidden;
